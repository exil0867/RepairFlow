import SignOut from '@/components/sign-out'
import prisma from '@/lib/prisma'

export default async function DisplayApplications() {
  // const items = await prisma.user.findMany()
  return (
    <>
      {/* {items.map((item) => {
        return item.id
      })} */}
    </>
  )
}
